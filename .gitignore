# Xcode
.DS_Store
build/
DerivedData/
*.xcuserstate
*.xcuserdatad/
xcuserdata/

# Secrets — never commit
EusoTrip.xcconfig
EusoTrip.xcconfig.local
AuthKey_*.p8
*.p8
asc_feedback_latest.log

# Licensed HERE Navigate package — provision from the approved vendor archive
# and verify against the offline supply-chain manifest before each release.
Vendor/HERE/*.zip
Vendor/HERE/heresdk.xcframework/
.build_copy.log
.build_copy2.log

# Local App Store Connect feedback cache
tmp/asc/
.asc/

# Swift Package Manager
.swiftpm/
Package.resolved

# Local build derived data (xcodebuild -derivedDataPath ./DD)
DD/
