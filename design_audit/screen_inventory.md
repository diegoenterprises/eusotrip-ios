# Screen Inventory

All paths relative to `/EusoTrip/`. AI-tell score 0–5 (header+bullet subtitle, SF-symbol-in-gray-tile rows, full-width gradient button per card, mixed badge systems, inconsistent radius/shadow).

| File | Screen | Header style | Badge style | Card style | AI-tells | Unique quirks |
|---|---|---|---|---|---|---|
| `Views/Driver/010_DriverHome.swift` | **Home (canonical)** | Personalized 2-col: "Hey, {name}" gradient 40/heavy + right-rail time+location | `StatusPill` (info/neutral/hazmat) + `HOT` gradient capsule | `ActiveCard` (xl=20, border 1.5 gradient) + `.eusoCard()` outline | 0 | Greeting is 2-column, not title+subtitle |
| `Views/Driver/DriverTabPanes.swift` L222 `DriverTripsPane` | **Eusoboards** | `topBar(title:"Eusoboards", subtitle:"AVAILABLE FREIGHT · BOOK & DISPATCH")` gradient 40/heavy | `StatusPill(equipment,.info)` + `HOT` gradient + hazmat | `.eusoCard()` for load cards | 2 | Title+bullet subtitle = AI-tell #1; full-width gradient "Book now" on every card |
| `Views/Driver/DriverTabPanes.swift` L1286 `MyLoadsSheet` | **My Loads Sheet** (from Eusoboards CTA) | In-sheet 28/heavy gradient "My Loads" + "ACTIVE · PENDING · FINISHED" | `StatusPill` per bucket + segmented gradient capsule | `.eusoCard()` via `MyLoadCard` | 2 | Bullet subtitle; segmented control uses gradient capsule + gradient underline (OK), but card is `.eusoCard()` while Home hero uses `ActiveCard` — mismatched container for same content |
| `Views/Driver/DriverTabPanes.swift` L1653 `DriverLoadsPane` | **My Loads tab** | 40/heavy gradient "My Loads" + "ACTIVE · PENDING · FINISHED · MAINTENANCE" | Same as MyLoadsSheet | Zeun card is a solid-gradient FILL card (not `.eusoCard` outline); load cards are `.eusoCard` | 3 | Zeun card is a "Book now"-class full-gradient card — breaks Home's outline-only doctrine; 4-term bullet subtitle |
| `Views/Driver/DriverTabPanes.swift` L1929 `DriverWalletPane` | **Wallet / EusoWallet** | `topBar(title:"Wallet", subtitle:"EUSOWALLET · SETTLEMENTS · PAYOUTS")` | `StatusPill("Stripe linked",.info)`; +/− colored amounts | `ActiveCard` hero, `.eusoCard()` sections, flat `palette.bgCard`+`borderFaint` tiles for quick actions | 2 | Bullet subtitle; mixes three card treatments in one screen (ActiveCard, eusoCard, bgCard+borderFaint) |
| `Views/Driver/DriverTabPanes.swift` L2596 `DriverMePane` | **Me** | `topBar(title:"Me", subtitle:"CAREER · COMPLIANCE · REPUTATION")` | none in hub | Profile header = `ActiveCard`; 15-row list in flat `palette.bgCard` + `borderFaint` + `Radius.lg` — NOT `.eusoCard()` | 4 | The archetype AI-tell: title+bullet subtitle + 15 rows of `palette.tintNeutral` gray-square SF symbols. Row glyph tile: 36x36, `Radius.md`, `palette.tintNeutral`, 14pt semibold primary-color glyph — the exact "AI template" tell |
| `Views/Driver/HotZonesWidget.swift` L984 `HotZonesWidget` | **Hot Zones (home widget)** | Inline: gradient `flame.fill` 24x24 tile + "HOT ZONES" micro + pulsing red "LIVE" dot | Pulse chips colored by semantic (green/red/amber) | `ActiveCard` + an inner heatmap `.strokeBorder(LinearGradient.diagonal, 1.25)` | 1 | Pulse chips use flat `palette.bgCardSoft` tiles, not `.eusoCard` — slight inconsistency |
| `Views/Driver/HotZonesWidget.swift` L1436 `HotZonesDetailSheet` | **Hot Zones detail sheet** | `NavigationStack` + `.navigationTitle("Zone Detail")` inline | `demand.label` capsule w/ semantic tint | `ActiveCard` sections | 1 | Uses NavigationStack title instead of gradient 40/heavy+subtitle header — breaks app-wide header language |
| `Views/Driver/HotZonesWidget.swift` L1831 `HotZonesListSheet` | **Hot Zones list sheet** | `NavigationStack` + `.navigationTitle("Hot Zones")` inline | Flame glyph in `demand.color.opacity(0.18)` circle + bespoke demand caps text | Bespoke rows: `palette.bgCard` + `borderFaint`, `Radius.md` | 2 | NavigationStack title (not gradient); two sheets (List + Detail) ship different chrome from the panes |
| `Views/Driver/MeDetailScreens.swift` L118 `MeDetailContainer` | **Me sub-sheet chrome** | 28/heavy SOLID (not gradient) title + UPPERCASE subtitle | — | bgPage | 2 | Title is `palette.textPrimary`, NOT gradient — Home's hero is gradient; this was inconsistent even within the Me family until the pane itself went gradient. Only 28pt vs panes' 40pt |
| `Views/Driver/MeDetailScreens.swift` L225 `MeEarningsView` | Earnings (now routes to Wallet) | inherited from container | `StatusPill("On pace",.success)` | `ActiveCard` + `MetricTile` pairs | 0 | Legacy, replaced by DriverWalletPane at runtime |
| `Views/Driver/MeDetailScreens.swift` L304 `MeTaxView` | **Tax / W-9 / 1099** | inherited | `StatusPill` | `ActiveCard` + list rows | 1 | — |
| `Views/Driver/MeDetailScreens.swift` L478 `MeAvailabilityView` | **Availability** | inherited | — | `ActiveCard` | 0 | — |
| `Views/Driver/MeDetailScreens.swift` L1146 `MeMissionsView` | **Missions** | inherited | `StatusPill` | `ActiveCard` + `.eusoCard()` | 1 | — |
| `Views/Driver/MeDetailScreens.swift` L1491 `MeRewardsView` | **Rewards** | inherited | catalog chips | `ActiveCard` | 1 | — |
| `Views/Driver/MeDetailScreens.swift` L1803 `MeBadgesView` | **Badges** | inherited | bespoke rosette chips | `ActiveCard` grid | 1 | — |
| `Views/Driver/MeDetailScreens.swift` L1868 `MeReferralsView` | **Referrals** | inherited | `StatusPill` | `ActiveCard` + CTA | 0 | — |
| `Views/Driver/MeDetailScreens.swift` L1945 `MeZeunView` | **Zeun Mechanics** | inherited (28/heavy solid) | `StatusPill("Healthy",.success)` + diagnostics semantic pills | `ActiveCard` + `.eusoCard` for diag list | 1 | 52pt gradient score echoes Home hero — good |
| `Views/Driver/MeDetailScreens.swift` L2634 `MeFleetView` | **Fleet Management** | inherited | `StatusPill` | `ActiveCard` grid | 1 | — |
| `Views/Driver/MeDetailScreens.swift` L2864 `MeHaulView` | **The Haul** | inherited 28/heavy solid, uses in-card caps header "THE HAUL · SEASON APRIL" | flame-warning capsule (hand-rolled) + `StatusPill("Top 3%",.info)` + bespoke gradient/bgCardSoft tab capsules | `ActiveCard` hero + `.eusoCard` leaderboard + tab-switched bodies | 2 | Bespoke flame-streak pill (orange capsule w/ flame) is not `StatusPill`; tab capsule is another gradient-fill card |
| `Views/Driver/MeDetailScreens.swift` L3015 `HaulLobbyTab` | **The Lobby** | embedded in Haul; role-tinted chat | role colors: driver=info, dispatch=success, fleet=warning, staff=magenta | message bubbles (bespoke, not shared) | 2 | Own role-color palette; message bubble doesn't use `.eusoCard` or any shared surface |
| `Views/Driver/MeDetailScreens.swift` L3369 `MeSettingsView` | **Settings** | inherited | `StatusPill` | `ActiveCard` (profile) + `.eusoCard()` toggle groups | 1 | Toggle uses `GradientToggleStyle` (good) |
| `Views/Driver/MeDetailScreens.swift` L3545 `MePulseView` | **EusoTrip Pulse** | inherited | `StatusPill` | `ActiveCard` | 1 | — |
| `Views/Driver/MeNotificationsView.swift` L162 `MeNotificationsView` | **Notifications** | inherited | `StatusPill` + gradient hero 28pt | `ActiveCard` statusHero + sectioned cards | 1 | Hero title is 28pt gradient (not 52pt); inconsistent with Wallet/Haul 52pt numerics |
| `Views/Driver/MeNewsView.swift` L26 `MeNewsView` | **Driver Intel** | owns its own ScrollView (bypasses container gutter) | bespoke category pills | bespoke article cards | 2 | Only Me child that bypasses `MeDetailContainer` padding — inconsistent with the rest |
| `Views/Driver/ELDIntegrationView.swift` L40 `ELDIntegrationView` | **ELD Integration** | `NavigationStack` `.navigationTitle("ELD Integration")` inline — NO gradient hero | `StatusPill("Connected",.success)` | `ActiveCard` | 2 | No 40/heavy gradient header, breaks pattern |
| `Views/Driver/MeComplianceView.swift` L322 `ComplianceInlinePanel` | **Compliance panels** (inline, not a screen) | inline banner rule | rule-severity chips | own card chrome | 1 | Not a top-level screen; embedded. Uses its own chip language |
| `Views/Driver/ProfileEditView.swift` | **Edit Profile** | `NavigationStack` inline title | — | form rows | 2 | No gradient header |
| `Views/Driver/DriverConversationView.swift` | **Conversation** | custom header w/ name + presence dot | role pills | message bubbles | 1 | Custom chrome (appropriate for chat) |
| `Views/Driver/MeNotificationsView.swift` (inbox category rows) | Category toggles | inline | — | `.eusoCard` | 0 | — |
| `Views/Auth/001_SignIn.swift` | **Sign In** | `GradientLogo` + `EType.h1` "Welcome back" centered | — | `GlassCard` (Theme/Glass.swift) | 0 | Different family (auth) — OK |
| `Views/Auth/002_CreateAccount.swift` | Create Account | same GlassCard family | — | `GlassCard` | 0 | — |
| `Views/Auth/003_ForgotPassword.swift` | Forgot Password | GlassCard family | — | `GlassCard` | 0 | — |
| `Views/Auth/004_ResetPassword.swift` | Reset Password | GlassCard family | — | `GlassCard` | 0 | — |
| `Views/Auth/005_TermsOfService.swift` | Terms | scrollable long text | — | — | 0 | — |
| `Views/Auth/006_PrivacyPolicy.swift` | Privacy | scrollable long text | — | — | 0 | — |
| `Views/Intro/IntroSplash.swift` | Splash | Lottie | — | — | 0 | — |
| `Views/Components/LoadDetailSheet.swift` | **Load Detail sheet** | in-sheet header | `StatusPill` + bespoke `HOT LANE` text at L108 | mix | 2 | Has its own "HOT LANE" label — another badge variant |
| `Views/Components/SOSEmergencySheet.swift` | SOS | sheet hero | danger pills | bespoke red cards | 1 | Appropriate urgency treatment |
| `Views/Driver/DriverNavController.swift` | Nav router | — | — | — | 0 | Controller |
| 011–048 journey screens (`011_PretripDVIR` → `048_ArrivalGateTaskActive`) | 38 lifecycle screens | `Shell` wrapper; each has its own bespoke top-bar, ActiveCard-style content | mixed `StatusPill` + hand-rolled pills (HAZMAT+TANK, HOT, etc.) | mix of `ActiveCard`, flat `palette.bgCard`, bespoke map overlays | avg 2 | Per-screen chrome — a 2nd design language in parallel to the Home/pane family |

**Total top-level screens surveyed:** 44 unique Swift view files (4 auth, 1 intro, 3 tab panes hosting ~25 Me sub-sheets, 2 Hot Zones sheets, 1 ELD, 1 profile edit, 1 SOS, 1 Load Detail, 1 conversation, 38 lifecycle screens).
